<!DOCTYPE html>
<html lang="en">
    <%- include("../includes/head.ejs")  %> 

<body>
    <%- include("../includes/header.ejs") %>
    <main>
        <form method="post" action="/predios/edit/<%= predio.id %>?m=PUT" >
            <h3>Editar Predio <%= predio.name %> </h3>
            <label for="name">Nombre del Predio</label>
            <input type="text" name="name" id="name"
            value="<%=locals.oldData ? locals.oldData.name : predio.name%>">
            <% if (locals.errors && errors.name) { %>
                <p class="msg-error"><%= errors.name.msg %></p>
            <% } %>

            <label for="adress">Dirección</label>
            <input type="text" name="adress" id="adress"
            value="<%=locals.oldData ? locals.oldData.adress : predio.adress%>">
            <% if (locals.errors && errors.adress) { %>
                <p class="msg-error"><%= errors.adress.msg %></p>
            <% } %>

            <label for="map">URL Google maps</label>
            <input type="text" name="map" id="map"
            value="<%=locals.oldData ? locals.oldData.map : predio.map%>">
            <% if (locals.errors && errors.map) { %>
                <p class="msg-error"><%= errors.map.msg %></p>
            <% } %>
            

            <button type="submit">Guardar Cambios</button>
        </form>

    </main>
</body>
</html>