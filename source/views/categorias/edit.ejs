<!DOCTYPE html>
<html lang="en">
    <%- include("../includes/head.ejs")  %> 

<body>
    <%- include("../includes/header.ejs") %>
    <main>
        <form method="post" action="/categorias/edit/<%= categoria.id %>?m=PUT">
            <h3> Editar Categoria <%= categoria.name %> </h3>
            <label for="name">Nombre de la categoria</label>
            <input type="text" name="name" id="name"
            value="<%= locals.oldData ? oldData.name: categoria.name %>">
            <% if (locals.errors && errors.name) { %>
                <p class="msg-error"><%= errors.name.msg %></p>
            <% } %>  

            <label for="description">Descripcion (edad maxima, etc)</label>
            <textarea name="description" id="description" ><%= categoria.description %> </textarea>

            <button type="submit">Actualizar datos</button>
        </form>
        <form action="/categorias/delete/<%=categoria.id%>?m=DELETE" method="post">
            <button type="submit">
                <i class="fa-solid fa-xmark"></i>
            </button>
        </form>
    </main>
</body>
</html>